---
- hosts: nginx2
  become: true
  tasks:
    - name: Setup nginx conf
      template:
        src=media.conf.tpl
        dest=/etc/nginx/sites-available/media.conf
      notify: restart nginx
  handlers:
    - name: restart nginx
      service:
        name=nginx
        state=restarted